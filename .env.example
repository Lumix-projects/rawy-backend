# MongoDB
MONGODB_URI=mongodb://localhost:27017/raawy

# Redis (Bull queues). Required for async email. Leave empty to skip.
REDIS_URL=redis://localhost:6379

# SMTP - Required for sending verification & password reset emails
# Gmail: Enable 2FA, then create App Password at https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-gmail@gmail.com
SMTP_PASS=xxxx-xxxx-xxxx-xxxx
EMAIL_FROM=your-gmail@gmail.com

# Frontend base URL for email links (e.g. rawy:// for app deep link)
FRONTEND_BASE_URL=http://localhost:8081

# JWT
JWT_SECRET=change-me-in-production
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d
<<<<<<< HEAD

# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# AWS S3 (avatar upload)
AWS_REGION=us-east-1
AWS_S3_BUCKET=raawy-avatars

# AWS S3 (podcast platform: audio + covers)
AWS_S3_BUCKET_AUDIO=raawy-audio
AWS_S3_BUCKET_COVERS=raawy-covers

# Upload limits (10 uploads per Creator per day)
UPLOAD_LIMIT_PER_DAY=10

# Server
PORT=3000
NODE_ENV=development

# Share links (episode deep link base URL for mobile/app)
APP_BASE_URL=http://localhost:3000
=======
>>>>>>> eddef33729badb4bea3f2589987efe7cb9023a6e
